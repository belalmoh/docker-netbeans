This repository contains docker configuration code that let you run netbeans-se (java ide) within docker.

**This configuration works with docker-compose**

You just need to run the following for it to work:
`docker compose up`